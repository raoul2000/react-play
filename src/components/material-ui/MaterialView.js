import React from 'react';
import Button from '@material-ui/core/Button';

const MaterialView = () => {
    console.log('material-ui demo');

    return (<div>
        <Button variant="contained" color="primary">
            Hello World
    </Button>
    </div>);
};

export default MaterialView;